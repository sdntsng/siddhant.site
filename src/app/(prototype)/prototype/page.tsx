import { PrototypeHub } from "@/components/prototypes/prototype-hub";

export const metadata = {
  title: "Prototypes Overview",
};

export default function PrototypeIndexPage() {
  return <PrototypeHub />;
}
